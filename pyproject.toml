[project]
name = "rp"
version = "0.1.2"
description = "Generate rhyming passwords"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["english-words>=2.0.2", "pronouncing>=0.2.0", "textual>=0.80"]

[project.scripts]
rp = "main:main"
